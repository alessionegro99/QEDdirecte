#pragma once

#include "U1.hpp"
#include "gauge_conf.hpp"

/* U1 staple(configuration &gc, physParams pp, size_t r, size_t mu)
{
    size_t index(size_t ST_DIM, size_t r, size_t mu)
    {
        return (r*ST_DIM)%(pow(r)) + mu
    }
} */